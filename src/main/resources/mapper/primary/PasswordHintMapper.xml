<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.wisead.mapper.primary.PasswordHintMapper">

    <!-- PasswordHint ResultMap -->
    <resultMap id="PasswordHintResultMap" type="kr.wisead.domain.user.entity.PasswordHint">
        <id property="seq" column="SEQ"/>
        <result property="userSeq" column="USER_SEQ"/>
        <result property="hintQuestion" column="HINT_QUESTION"/>
        <result property="hintAnswer" column="HINT_ANSWER"/>
        <result property="regDate" column="REG_DATE"/>
        <result property="uptDate" column="UPT_DATE"/>
    </resultMap>

    <!-- 사용자 시퀀스로 비밀번호 힌트 조회 -->
    <select id="findByMngSeq" resultMap="PasswordHintResultMap">
        SELECT SEQ, USER_SEQ, HINT_QUESTION, HINT_ANSWER, REG_DATE, UPT_DATE
        FROM user_password_hint
        WHERE USER_SEQ = #{userSeq}
    </select>

    <!-- 비밀번호 힌트 등록 -->
    <insert id="insert" parameterType="kr.wisead.domain.user.entity.PasswordHint" useGeneratedKeys="true" keyProperty="seq">
        INSERT INTO user_password_hint (USER_SEQ, HINT_QUESTION, HINT_ANSWER, REG_DATE)
        VALUES (#{userSeq}, #{hintQuestion}, #{hintAnswer}, NOW())
    </insert>

    <!-- 비밀번호 힌트 수정 -->
    <update id="update" parameterType="kr.wisead.domain.user.entity.PasswordHint">
        UPDATE user_password_hint
        SET HINT_QUESTION = #{hintQuestion},
            HINT_ANSWER = #{hintAnswer},
            UPT_DATE = NOW()
        WHERE USER_SEQ = #{userSeq}
    </update>

    <!-- 비밀번호 힌트 검증 -->
    <select id="verifyHint" resultType="int">
        SELECT COUNT(*)
        FROM user_password_hint
        WHERE USER_SEQ = #{userSeq}
          AND HINT_QUESTION = #{hintQuestion}
          AND HINT_ANSWER = #{hintAnswer}
    </select>

</mapper>
