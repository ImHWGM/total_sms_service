-- =================================================================
-- WiseAd 보안/컴플라이언스 PR1: Data Migration Rollback
-- =================================================================
-- 용도: 20260521_01_data_migration.sql 적용 취소
-- 주의: LIFECYCLE_STATUS 를 DEFAULT 'ACTIVE' 로 되돌림.
--       STATUS 한글 값은 그대로 보존되므로 로그인은 legacy path 로 동작.
-- =================================================================

-- -----------------------------------------------------------------
-- 1) LIFECYCLE_STATUS 를 PR0 추가 시 기본값인 'ACTIVE' 로 리셋
--    (PR0 DDL DEFAULT 'ACTIVE' 와 일치)
-- -----------------------------------------------------------------
UPDATE `user`
SET `LIFECYCLE_STATUS` = 'ACTIVE';

-- -----------------------------------------------------------------
-- 2) 신규 정책으로 설정된 LOCKED_UNTIL 을 NULL 로 초기화
--    (PR0 rollback 전에 실행하거나, PR1 단독 롤백 시 사용)
-- -----------------------------------------------------------------
UPDATE `user`
SET `LOCKED_UNTIL` = NULL;
