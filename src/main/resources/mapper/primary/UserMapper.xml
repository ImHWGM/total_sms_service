<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.wisead.mapper.primary.UserMapper">

    <!-- User ResultMap -->
    <resultMap id="UserResultMap" type="kr.wisead.domain.user.entity.User">
        <id property="seq" column="SEQ"/>
        <result property="userId" column="USER_ID"/>
        <result property="userPass" column="USER_PASS"/>
        <result property="corpName" column="CORP_NAME"/>
        <result property="corpAddr" column="CORP_ADDR"/>
        <result property="bizNum" column="BIZ_NUM"/>
        <result property="bizTel" column="BIZ_TEL"/>
        <result property="person" column="PERSON"/>
        <result property="phone" column="PHONE"/>
        <result property="email" column="EMAIL"/>
        <result property="userLevel" column="USER_LEVEL"/>
        <result property="useYn" column="USE_YN"/>
        <result property="allowIpYn" column="ALLOW_IP_YN"/>
        <result property="allowIp" column="ALLOW_IP"/>
        <result property="lastLogin" column="LAST_LOGIN"/>
        <result property="loginFailureCnt" column="LOGIN_FAILURE_CNT"/>
        <result property="regDate" column="REG_DATE"/>
        <result property="regId" column="REG_ID"/>
        <result property="uptDate" column="UPT_DATE"/>
        <result property="uptId" column="UPT_ID"/>
        <result property="status" column="STATUS"/>
        <result property="emailCode" column="EMAIL_CODE"/>
        <result property="codeValidate" column="CODE_VALIDATE"/>
        <result property="callback" column="CALLBACK"/>
        <result property="bizPdfLoc" column="BIZ_PDF_LOC"/>
        <result property="storeCode" column="STORE_CODE"/>
        <result property="blockedSeq" column="BLOCKED_SEQ"/>
        <result property="loginPhone" column="LOGIN_PHONE"/>
        <result property="defaultTwoFactorMethod" column="DEFAULT_TWO_FACTOR_METHOD"/>
        <!-- PR1: 신규 컬럼 (PR0 additive schema) -->
        <result property="lockedUntil" column="LOCKED_UNTIL"/>
        <result property="lifecycleStatus" column="LIFECYCLE_STATUS"
                typeHandler="org.apache.ibatis.type.EnumTypeHandler"/>
        <result property="dormantAt" column="DORMANT_AT"/>
        <result property="dormantNotifiedAt" column="DORMANT_NOTIFIED_AT"/>
        <result property="withdrawnAt" column="WITHDRAWN_AT"/>
        <result property="anonymizedAt" column="ANONYMIZED_AT"/>
    </resultMap>

    <!-- 기본 컬럼 (PR1: 신규 컬럼 포함) -->
    <sql id="userColumns">
        SEQ, USER_ID, USER_PASS, CORP_NAME, CORP_ADDR, BIZ_NUM, BIZ_TEL,
        PERSON, PHONE, EMAIL, USER_LEVEL, USE_YN, ALLOW_IP_YN, ALLOW_IP,
        LAST_LOGIN, LOGIN_FAILURE_CNT, REG_DATE, REG_ID, UPT_DATE, UPT_ID,
        STATUS, EMAIL_CODE, CODE_VALIDATE, CALLBACK,
        BIZ_PDF_LOC, STORE_CODE, BLOCKED_SEQ,
        LOGIN_PHONE, DEFAULT_TWO_FACTOR_METHOD,
        LOCKED_UNTIL, LIFECYCLE_STATUS, DORMANT_AT, DORMANT_NOTIFIED_AT, WITHDRAWN_AT, ANONYMIZED_AT
    </sql>

    <!-- 회원 조회 (by SEQ) -->
    <select id="findBySeq" resultMap="UserResultMap">
        SELECT <include refid="userColumns"/>
        FROM user
        WHERE SEQ = #{seq}
    </select>

    <!-- 회원 조회 (by USER_ID) -->
    <select id="findByUserId" resultMap="UserResultMap">
        SELECT <include refid="userColumns"/>
        FROM user
        WHERE USER_ID = #{userId}
    </select>

    <!-- SEQ로 USER_ID 조회 -->
    <select id="findUserIdBySeq" resultType="string">
        SELECT USER_ID
        FROM user
        WHERE SEQ = #{seq}
    </select>

    <!-- USER_ID로 SEQ 조회 -->
    <select id="findSeqByUserId" resultType="int">
        SELECT SEQ
        FROM user
        WHERE USER_ID = #{userId}
    </select>

    <!-- 회원 조회 (by EMAIL) -->
    <select id="findByEmail" resultMap="UserResultMap">
        SELECT <include refid="userColumns"/>
        FROM user
        WHERE EMAIL = #{email}
    </select>

    <!-- 아이디 중복 확인 -->
    <select id="existsByUserId" resultType="boolean">
        SELECT EXISTS(SELECT 1 FROM user WHERE USER_ID = #{userId})
    </select>

    <!-- 이메일 중복 확인 -->
    <select id="existsByEmail" resultType="boolean">
        SELECT EXISTS(SELECT 1 FROM user WHERE EMAIL = #{email})
    </select>

    <!-- 회원가입 -->
    <insert id="insert" parameterType="kr.wisead.domain.user.entity.User" useGeneratedKeys="true" keyProperty="seq">
        INSERT INTO user (
            USER_ID, USER_PASS, CORP_NAME, CORP_ADDR, BIZ_NUM, BIZ_TEL,
            PERSON, PHONE, EMAIL, USER_LEVEL, USE_YN, STATUS,
            REG_DATE, REG_ID, STORE_CODE,
            LOGIN_PHONE, DEFAULT_TWO_FACTOR_METHOD
        ) VALUES (
            #{userId}, #{userPass}, #{corpName}, #{corpAddr}, REPLACE(#{bizNum}, '-', ''), #{bizTel},
            #{person}, #{phone}, #{email}, #{userLevel}, #{useYn}, #{status},
            NOW(), #{regId}, #{storeCode},
            #{loginPhone}, #{defaultTwoFactorMethod}
        )
    </insert>

    <!-- 회원정보 수정 -->
    <update id="update" parameterType="kr.wisead.domain.user.entity.User">
        UPDATE user
        SET
            CORP_NAME = #{corpName},
            CORP_ADDR = #{corpAddr},
            BIZ_NUM = REPLACE(#{bizNum}, '-', ''),
            BIZ_TEL = #{bizTel},
            PERSON = #{person},
            PHONE = #{phone},
            EMAIL = #{email},
            CALLBACK = #{callback},
            UPT_DATE = NOW(),
            UPT_ID = #{uptId}
        WHERE SEQ = #{seq}
    </update>

    <!-- 로그인 성공 시 최근 로그인 시간 업데이트 -->
    <update id="updateLastLogin">
        UPDATE user
        SET LAST_LOGIN = NOW(),
            LOGIN_FAILURE_CNT = 0
        WHERE USER_ID = #{userId}
    </update>

    <!-- 로그인 실패 횟수 원자적 증가 + 5회차 정확히 한 번만 잠금 설정 (PR1 AC27).
         WHEN LOGIN_FAILURE_CNT + 1 = 5 → exactly-once (sliding window 회피).
         6회차+: LOCKED_UNTIL 미변경. -->
    <update id="increaseLoginFailureCnt">
        UPDATE user
        SET LOGIN_FAILURE_CNT = LOGIN_FAILURE_CNT + 1,
            LOCKED_UNTIL = CASE
                WHEN LOGIN_FAILURE_CNT + 1 = 5 THEN UTC_TIMESTAMP() + INTERVAL 10 MINUTE
                ELSE LOCKED_UNTIL
            END
        WHERE SEQ = #{seq}
    </update>

    <!-- 로그인 성공/잠금 해제 시 실패 횟수 초기화 + 잠금 해제 (원자적) -->
    <update id="resetLoginAndUnlock">
        UPDATE user
        SET LOGIN_FAILURE_CNT = 0,
            LOCKED_UNTIL = NULL
        WHERE SEQ = #{seq}
    </update>

    <!-- 로그인 실패 횟수 초기화 (레거시 — userId 기반; 기존 코드 호환) -->
    <update id="resetLoginFailureCnt">
        UPDATE user
        SET LOGIN_FAILURE_CNT = 0
        WHERE USER_ID = #{userId}
    </update>

    <!-- 관리자가 특정 잠금 시각 직접 설정 (관리자 임시 연장 등 운영 용도) -->
    <update id="setLockedUntil">
        UPDATE user
        SET LOCKED_UNTIL = #{lockedUntil}
        WHERE SEQ = #{seq}
    </update>

    <!-- 비밀번호 변경 -->
    <update id="updatePassword">
        UPDATE user
        SET USER_PASS = #{userPass},
            UPT_DATE = NOW()
        WHERE USER_ID = #{userId}
    </update>

    <!-- 이메일 인증 코드 저장 -->
    <update id="updateEmailCode">
        UPDATE user
        SET EMAIL_CODE = #{emailCode},
            CODE_VALIDATE = #{codeValidate}
        WHERE USER_ID = #{userId}
    </update>

    <!-- 회원 상태 변경 -->
    <update id="updateStatus">
        UPDATE user
        SET STATUS = #{status},
            UPT_DATE = NOW()
        WHERE USER_ID = #{userId}
    </update>

    <!-- 회원 목록 조회 (페이징) -->
    <select id="findAll" resultMap="UserResultMap">
        SELECT <include refid="userColumns"/>
        FROM user
        WHERE USE_YN = 'Y'
        ORDER BY SEQ DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <!-- 회원 수 조회 -->
    <select id="count" resultType="long">
        SELECT COUNT(*) FROM user WHERE USE_YN = 'Y'
    </select>

    <!-- 아이디 찾기 -->
    <select id="findByEmailAndPerson" resultMap="UserResultMap">
        SELECT <include refid="userColumns"/>
        FROM user
        WHERE EMAIL = #{email}
          AND PERSON = #{person}
    </select>

    <!-- 계정 잠금 해제 -->
    <update id="unlockAccount">
        UPDATE user
        SET LOGIN_FAILURE_CNT = 0,
            STATUS = '승인',
            UPT_DATE = NOW()
        WHERE USER_ID = #{userId}
    </update>

    <!-- 비밀번호 만료일 연장 (UPT_DATE 갱신으로 180일 연장) -->
    <update id="extendPasswordExpiry">
        UPDATE user
        SET UPT_DATE = NOW()
        WHERE USER_ID = #{userId}
    </update>

    <!-- 비밀번호 초기화 (관리자용) -->
    <update id="resetPassword">
        UPDATE user
        SET USER_PASS = #{userPass},
            LOGIN_FAILURE_CNT = 0,
            UPT_DATE = NOW()
        WHERE USER_ID = #{userId}
    </update>

    <!-- 상점코드 조회 (광고문자 수신거부용) -->
    <select id="selectStoreCodeByUserId" resultType="string">
        SELECT STORE_CODE
        FROM user
        WHERE USER_ID = #{userId}
    </select>

    <!-- 비밀번호 찾기용 회원 조회 -->
    <select id="findByUserIdAndCorpNameAndPersonAndPhone" resultMap="UserResultMap">
        SELECT <include refid="userColumns"/>
        FROM user
        WHERE USER_ID = #{userId}
          AND CORP_NAME = #{corpName}
          AND PERSON = #{person}
          AND PHONE = #{phone}
          AND USE_YN = 'Y'
    </select>

    <!-- 회원 삭제 (USE_YN = 'N' 처리) -->
    <update id="deleteBySeq">
        UPDATE user
        SET USE_YN = 'N',
            UPT_DATE = NOW()
        WHERE SEQ = #{seq}
    </update>

    <!-- 회원 일괄 삭제 (USE_YN = 'N' 처리) -->
    <update id="deleteBySeqList">
        UPDATE user
        SET USE_YN = 'N',
            UPT_DATE = NOW()
        WHERE SEQ IN
        <foreach collection="seqList" item="seq" open="(" separator="," close=")">
            #{seq}
        </foreach>
    </update>

    <!-- 회원 정보 수정 (기업 정보) -->
    <update id="updateMemberInfo" parameterType="kr.wisead.domain.user.entity.User">
        UPDATE user
        SET
            CORP_NAME = #{corpName},
            CORP_ADDR = #{corpAddr},
            BIZ_NUM = REPLACE(#{bizNum}, '-', ''),
            BIZ_TEL = #{bizTel},
            PERSON = #{person},
            PHONE = #{phone},
            EMAIL = #{email},
            USER_LEVEL = #{userLevel},
            ALLOW_IP_YN = #{allowIpYn},
            ALLOW_IP = #{allowIp},
            STATUS = #{status},
            CALLBACK = #{callback},
            LOGIN_PHONE = #{loginPhone},
            DEFAULT_TWO_FACTOR_METHOD = #{defaultTwoFactorMethod},
            UPT_DATE = NOW(),
            UPT_ID = #{uptId}
        WHERE SEQ = #{seq}
    </update>

    <!-- 아이디 찾기용 회원 조회 (기업명, 담당자명, 연락처) -->
    <select id="findByCorpNameAndPersonAndPhone" resultMap="UserResultMap">
        SELECT <include refid="userColumns"/>
        FROM user
        WHERE CORP_NAME = #{corpName}
          AND PERSON = #{person}
          AND PHONE = #{phone}
          AND USE_YN = 'Y'
    </select>

    <!-- 모든 활성 사용자 아이디 목록 조회 -->
    <select id="findAllUserIds" resultType="string">
        SELECT USER_ID
        FROM user
        WHERE USE_YN = 'Y'
        ORDER BY USER_ID
    </select>

    <!-- 상점코드 중복 확인 -->
    <select id="existsByStoreCode" resultType="boolean">
        SELECT EXISTS(SELECT 1 FROM user WHERE STORE_CODE = #{storeCode})
    </select>

    <!-- 상점코드로 사용자 ID 조회 (추가) -->
    <select id="findUserIdByStoreCode" resultType="string">
        SELECT USER_ID
        FROM user
        WHERE STORE_CODE = #{storeCode} LIMIT 1
    </select>

    <!-- 사용자ID 목록으로 상점코드 목록 조회 -->
    <select id="selectStoreCodesByUserIds" resultType="string">
        SELECT STORE_CODE
        FROM user
        WHERE USER_ID IN
        <foreach collection="userIds" item="userId" open="(" separator="," close=")">
            #{userId}
        </foreach>
        AND STORE_CODE IS NOT NULL
        AND STORE_CODE != ''
        AND USE_YN = 'Y'
    </select>

    <!-- 상점코드 목록으로 {storeCode → userId} 매핑 조회 -->
    <select id="findUserIdsByStoreCodesRaw" resultType="map">
        SELECT STORE_CODE, USER_ID
        FROM user
        WHERE STORE_CODE IN
        <foreach collection="storeCodes" item="storeCode" open="(" separator="," close=")">
            #{storeCode}
        </foreach>
        AND USE_YN = 'Y'
    </select>

    <!-- PR1: 탈퇴 처리 — LIFECYCLE_STATUS, WITHDRAWN_AT, ANONYMIZED_AT, EMAIL, PERSON 일괄 갱신 -->
    <update id="withdrawUser">
        UPDATE user
        SET LIFECYCLE_STATUS = #{lifecycleStatus},
            WITHDRAWN_AT     = #{withdrawnAt},
            ANONYMIZED_AT    = #{anonymizedAt},
            EMAIL            = #{email},
            PERSON           = #{person},
            UPT_DATE         = NOW()
        WHERE SEQ = #{seq}
    </update>

    <!-- 로그인 휴대폰번호 갱신 (마이페이지 SMS 2FA 등록/해제 — plan v5 §4 Phase E) -->
    <update id="updateLoginPhone">
        UPDATE user
        SET LOGIN_PHONE = #{loginPhone},
            UPT_DATE = NOW()
        WHERE SEQ = #{seq}
    </update>

    <!-- 기본 2FA 채널 갱신 (EMAIL/SMS — plan v5 §4 Phase E) -->
    <update id="updateDefaultTwoFactorMethod">
        UPDATE user
        SET DEFAULT_TWO_FACTOR_METHOD = #{method},
            UPT_DATE = NOW()
        WHERE SEQ = #{seq}
    </update>

    <!-- PR3: 휴면 사전 알림 대상 조회 (5개월 미접속 + ACTIVE + 알림 미발송, AC37 UTC) -->
    <select id="selectDormancyNotifyCandidates" resultMap="UserResultMap">
        SELECT <include refid="userColumns"/>
        FROM user
        WHERE LAST_LOGIN &lt; UTC_TIMESTAMP() - INTERVAL 5 MONTH
          AND LIFECYCLE_STATUS = 'ACTIVE'
          AND DORMANT_NOTIFIED_AT IS NULL
        LIMIT #{limit}
    </select>

    <!-- PR3: 휴면 전환 대상 조회 (6개월 미접속 + ACTIVE, AC37 UTC) -->
    <select id="selectDormancyTransitionCandidates" resultMap="UserResultMap">
        SELECT <include refid="userColumns"/>
        FROM user
        WHERE LAST_LOGIN &lt; UTC_TIMESTAMP() - INTERVAL 6 MONTH
          AND LIFECYCLE_STATUS = 'ACTIVE'
        LIMIT #{limit}
    </select>

    <!-- PR3: 배치 탈퇴 전환 대상 조회 (휴면 6개월 경과 + DORMANT, AC37 UTC) -->
    <select id="selectWithdrawalCandidates" resultMap="UserResultMap">
        SELECT <include refid="userColumns"/>
        FROM user
        WHERE DORMANT_AT &lt; UTC_TIMESTAMP() - INTERVAL 6 MONTH
          AND LIFECYCLE_STATUS = 'DORMANT'
        LIMIT #{limit}
    </select>

    <!-- PR3: 휴면 사전 알림 발송 시각 갱신 (idempotent — AC37 UTC) -->
    <update id="markDormantNotified">
        UPDATE user
        SET DORMANT_NOTIFIED_AT = UTC_TIMESTAMP(),
            UPT_DATE = NOW()
        WHERE SEQ = #{seq}
    </update>

    <!-- PR3: 휴면 전환 (LIFECYCLE_STATUS = DORMANT, DORMANT_AT = UTC_TIMESTAMP()) -->
    <update id="transitionToDormant">
        UPDATE user
        SET LIFECYCLE_STATUS = 'DORMANT',
            DORMANT_AT       = UTC_TIMESTAMP(),
            UPT_DATE         = NOW()
        WHERE SEQ = #{seq}
    </update>

    <!-- PR3: 배치 탈퇴 전환 + 익명화 (PIPA Art.21 — LIFECYCLE_STATUS = WITHDRAWN + 개인정보 마스킹) -->
    <update id="transitionToWithdrawnAndAnonymize">
        UPDATE user
        SET LIFECYCLE_STATUS = 'WITHDRAWN',
            WITHDRAWN_AT     = UTC_TIMESTAMP(),
            EMAIL            = CONCAT('anon_', SEQ, '@deleted'),
            PERSON           = CONCAT('anon_', SEQ),
            ANONYMIZED_AT    = UTC_TIMESTAMP(),
            UPT_DATE         = NOW()
        WHERE SEQ = #{seq}
    </update>

    <!-- PR3: 휴면 복구 (LIFECYCLE_STATUS = ACTIVE, DORMANT_AT = NULL, DORMANT_NOTIFIED_AT = NULL) -->
    <update id="recoverDormant">
        UPDATE user
        SET LIFECYCLE_STATUS      = 'ACTIVE',
            DORMANT_AT            = NULL,
            DORMANT_NOTIFIED_AT   = NULL,
            UPT_DATE              = NOW()
        WHERE SEQ = #{seq}
    </update>

</mapper>
