<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.wisead.mapper.primary.SmsSendMapper">

    <!-- 발송 이력 등록 -->
    <insert id="insert" parameterType="kr.wisead.domain.message.entity.SmsSend" useGeneratedKeys="true" keyProperty="seq">
        INSERT INTO sms_send (EVENT_SEQ, USER_SEQ, SUBJECT, CONTENT, SEND_TYPE, RECIVED_NUM, CALLBACK, REQ_DATE, SEND_YN, REG_DATE, REG_ID)
        VALUES (#{eventSeq}, #{userSeq}, #{subject}, #{content}, #{sendType}, #{receivedNum}, #{callback}, #{reqDate}, #{sendYn}, #{regDate}, #{regId})
    </insert>

    <!-- 해당 이벤트에서 발송 이력이 있는 USER_SEQ 목록 조회 -->
    <select id="selectSentUserSeqs" resultType="java.lang.Integer">
        SELECT DISTINCT USER_SEQ
        FROM sms_send
        WHERE EVENT_SEQ = #{eventSeq}
    </select>

    <!-- 단건 존재 확인 -->
    <select id="existsByEventSeqAndUserSeq" resultType="boolean">
        SELECT EXISTS (
            SELECT 1
            FROM sms_send
            WHERE EVENT_SEQ = #{eventSeq}
              AND USER_SEQ = #{userSeq}
        )
    </select>

    <!-- 이벤트+사용자 시퀀스로 발송 이력 삭제 -->
    <delete id="deleteByEventSeqAndUserSeq">
        DELETE FROM sms_send
        WHERE EVENT_SEQ = #{eventSeq}
          AND USER_SEQ = #{userSeq}
    </delete>

</mapper>
